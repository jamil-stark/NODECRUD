<%- include('layout/header') %>
<div class="container">
    <div class="row my 4">
        <div class="col-lg-12">
            <% if (message) { %>
                <div class="alert alert-dismissible fade show alert-<%= message.type %>" role="alert">
                    <button class="btn-close" type="button" data-bs-dimiss="alert" aria-label="close">
                    </button>
                    <strong>
                        <%= message.message %>
                    </strong>
                </div>

            <% } %>
        </div>
    </div>
</div>
<section class="body">
    <div>
        <h1>we are here to handle your personal <br> problems</h1>
        <a href="/add_request"><button>start now</button></a>
    </div>
</section>
<%- include('layout/footer') %>
